version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # You can override your Keycloak secret here for production
      # - KEYCLOAK_ADMIN_CLIENT_SECRET=your_production_secret
    restart: always